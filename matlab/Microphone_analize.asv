meas = load("calib.txt");

plot(meas);

mid = mean(meas);
disp(mid);
scope = max(meas) - min(meas);
disp(scope);

figure;
freq = fft(meas);
p2 = abs(freq / length(meas));
p1 = p2(1:length)
out = 1:length(meas)/1000;
plot(out,freq);
